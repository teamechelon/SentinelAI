# Scripts

Keep explicit developer entry points here, such as database initialization, deterministic scenario loading, model training, and evaluation. Scripts should call package services instead of duplicating business logic.

