# Configuration

Store reviewable configuration for rule thresholds, rule weights, sequence windows, model parameters, peer-group definitions, and deterministic demo scenarios here. Configuration must be versioned and referenced by every scored alert.

Do not store credentials or machine-specific secrets in this directory.

