# Generated artifacts

This directory contains rebuildable outputs rather than source code.

- `models/`: trusted, locally generated model/calibration artifacts and metadata.
- `reports/`: generated evaluation tables, plots, and run summaries.

Artifact payloads are ignored by Git. Keep version metadata, parameters, data checksums, and library versions in SQLite or versioned configuration so results remain reproducible.

